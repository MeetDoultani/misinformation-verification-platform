# Core data handling
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
scikit-learn>=1.3.0
pyarrow>=14.0.0
tabulate>=0.9.0

# Download / HTTP
requests>=2.31.0
tqdm>=4.66.0

# Text cleaning
regex>=2023.10.3
langdetect>=1.0.9
ftfy>=6.1.1

# EDA / plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# Image handling (for image forensics metadata/validation only)
Pillow>=10.0.0

# Testing
pytest>=7.4.0

# Optional: Kaggle CLI for gated dataset downloads (user must configure API token)
kaggle>=1.6.0
